// gostore — 단일 정적 바이너리로 배포되는 스토리지 랩 에이전트/컨트롤러/벤치마커.
//
// 외부 모듈 의존성을 의도적으로 0으로 유지한다. 노드에 배포되는 산출물이
// 파일 하나뿐이어야 랩의 목적(런타임·패키지매니저 없는 배포)이 성립한다.
// 의존성을 추가하기 전에 README.md 의 "의존성 정책"을 먼저 확인할 것.
module gostore

go 1.24
